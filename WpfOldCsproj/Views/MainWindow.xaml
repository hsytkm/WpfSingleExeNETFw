<Window
    x:Class="WpfOldCsproj.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="clr-namespace:WpfOldCsproj.Views"
    xmlns:vm="clr-namespace:WpfOldCsproj.ViewModels"
    Title="WpfOldCsproj"
    Width="400"
    Height="300"
    mc:Ignorable="d">

    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>

    <StackPanel Margin="10">
        <TextBlock Text="WPF using old csproj on .NET Framework" />

        <!--<Button Command="{Binding CountUpCommand}" Content="Count up" />
        <TextBlock Text="{Binding Counter}" />-->

        <TextBlock Text="{Binding TimeCounter.Value, StringFormat='Timer={0}'}" />

    </StackPanel>
</Window>
